{% sw_extends '@Storefront/storefront/page/checkout/confirm/confirm-shipping.html.twig' %}

{# Render the carrier picker only under our gate shipping method, so no other
   method (self-pickup, flat rate) shows it. Appended after the shipping-method
   form; the storefront plugin fills the container from
   /kommandhub/shipping/carriers. #}
{% block page_checkout_confirm_shipping_form %}
    {{ parent() }}
{% endblock %}
